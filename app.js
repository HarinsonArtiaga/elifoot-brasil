import {CLUBS,makeSquad} from './data.js'; import {emptyTable,playMatch,tableRows,schedule} from './engine.js';
const $=s=>document.querySelector(s); const money=n=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL',maximumFractionDigits:0}).format(n); let selected=0,state=null;
function fresh(){const clubs=Object.fromEntries(CLUBS.map(c=>[c.id,c])); return {selected,clubs,table:emptyTable(CLUBS),round:1,results:[],cash:clubs[selected].budget,squad:makeSquad(clubs[selected]),manager:'Você'} }
function setup(){ $('#app').innerHTML=`<section class="setup"><div class="panel hero"><p class="eyebrow">EDIÇÃO 2026 · INSPIRADO NOS MANAGERS CLÁSSICOS</p><h2>Você escala. O domingo responde.</h2><p>Uma experiência original com a simplicidade dos jogos de manager do início dos anos 2000: escolha seu clube, escale o time, atravesse o campeonato e tente sobreviver à diretoria.</p><div class="actions"><button class="primary" id="start">Assumir comando</button>${localStorage.getItem('cartola-save')?'<button id="continue">Continuar jogo salvo</button>':''}</div></div><div class="panel"><p class="eyebrow">ESCOLHA SEU CLUBE</p><div class="club-grid">${CLUBS.map(c=>`<button class="club-card ${c.id===selected?'selected':''}" data-club="${c.id}"><span class="strength">${c.strength}</span><b>${c.name}</b><small>${c.region} · caixa inicial ${money(c.budget)}</small></button>`).join('')}</div></div></section>`; document.querySelectorAll('[data-club]').forEach(b=>b.onclick=()=>{selected=+b.dataset.club;setup()}); $('#start').onclick=()=>{state=fresh();renderDash()}; $('#continue')?.addEventListener('click',()=>{state=JSON.parse(localStorage.getItem('cartola-save'));selected=state.selected;renderDash()}); }
function renderDash(){const club=state.clubs[selected]; $('#app').innerHTML=`<section class="dashboard"><aside class="panel sidebar"><div class="identity"><div class="crest">${club.short.slice(0,3)}</div><div><b>${club.name}</b><small class="mono">TEMPORADA 2026</small></div></div><nav class="nav"><button class="active" data-view="home">Visão geral</button><button data-view="squad">Elenco e escalação</button><button data-view="table">Classificação</button><button data-view="fixtures">Jogos</button></nav><div class="actions"><button id="save">Salvar</button><button id="reset" class="danger">Novo jogo</button></div></aside><div class="content"><div id="home" class="view active"></div><div id="squad" class="view"></div><div id="table" class="view"></div><div id="fixtures" class="view"></div></div></section>`; document.querySelectorAll('[data-view]').forEach(b=>b.onclick=()=>{document.querySelectorAll('.nav button').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.querySelectorAll('.view').forEach(x=>x.classList.remove('active'));$('#'+b.dataset.view).classList.add('active');renderAll()}); $('#save').onclick=()=>{localStorage.setItem('cartola-save',JSON.stringify(state)); toast('Jogo salvo neste navegador.')}; $('#reset').onclick=()=>{if(confirm('Começar uma nova temporada?'))setup()}; renderAll(); }
function renderAll(){const club=state.clubs[selected], rows=tableRows(state); $('#home').innerHTML=`<div class="stats"><div class="stat"><span class="eyebrow">RODADA</span><b>${state.round}/38</b></div><div class="stat"><span class="eyebrow">PONTOS</span><b>${state.table[selected].points}</b></div><div class="stat"><span class="eyebrow">POSIÇÃO</span><b>${rows.find(r=>r.clubId===selected).pos}º</b></div><div class="stat"><span class="eyebrow">CAIXA</span><b class="money">${money(state.cash)}</b></div></div><div class="panel"><p class="eyebrow">CENTRAL DO TREINADOR</p><h2>${state.results.length?'Último domingo':'A temporada começa agora'}</h2><p>${state.results.length?`${state.results.at(-1).home} ${state.results.at(-1).hg} × ${state.results.at(-1).ag} ${state.results.at(-1).away}`:'Escolha uma aba para conhecer seu elenco. Quando estiver pronto, avance a rodada.'}</p><div class="actions"><button class="primary" id="advance" ${state.round>38?'disabled':''}>Avançar rodada</button></div></div>`; $('#table').innerHTML=`<div class="panel"><p class="eyebrow">BRASILEIRÃO · CLASSIFICAÇÃO</p><div class="table-wrap"><table><thead><tr><th>#</th><th>Clube</th><th>J</th><th>V</th><th>E</th><th>D</th><th>SG</th><th>Pts</th></tr></thead><tbody>${rows.map(r=>`<tr class="${r.clubId===selected?'you':''}"><td>${r.pos}</td><td>${r.club.name}</td><td>${r.played}</td><td>${r.wins}</td><td>${r.draws}</td><td>${r.losses}</td><td>${r.goalsFor-r.goalsAgainst}</td><td><b>${r.points}</b></td></tr>`).join('')}</tbody></table></div></div>`; $('#squad').innerHTML=`<div class="panel"><p class="eyebrow">ELENCO · ${state.squad.length} ATLETAS</p><div class="squad">${state.squad.map(p=>`<article class="player"><b>${p.name}</b><small>${p.role} · ${p.age} anos</small><div class="bar"><i style="width:${p.rating}%"></i></div><small>Força ${p.rating} · Moral ${p.morale}</small></article>`).join('')}</div></div>`; const upcoming=Array.from({length:5},(_,i)=>{const opp=CLUBS[(selected+i+1)%20];return `<div class="fixture"><span><small>RODADA ${state.round+i}</small><br>${i%2?'Fora':'Casa'} · <b>${opp.name}</b></span><b>${i?'—':'PRÓXIMO'}</b></div>`}).join(''); $('#fixtures').innerHTML=`<div class="panel"><p class="eyebrow">CALENDÁRIO</p>${upcoming}</div>`; $('#advance')?.addEventListener('click',advance); }
function advance(){const opponents=CLUBS.filter(c=>c.id!==selected);const opp=opponents[(state.round-1)%opponents.length];const match=playMatch(state,selected,opp.id);state.results.push(match);state.round++;state.cash+=25000+(match.hg>=match.ag?12000:0);renderAll();toast(`Rodada encerrada: ${match.home} ${match.hg} × ${match.ag} ${match.away}`)} function toast(t){const n=document.createElement('div');n.className='notice';n.textContent=t;$('#app').prepend(n);setTimeout(()=>n.remove(),3500)} setup();
