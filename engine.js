export function emptyTable(clubs){return Object.fromEntries(clubs.map(c=>[c.id,{clubId:c.id,played:0,wins:0,draws:0,losses:0,goalsFor:0,goalsAgainst:0,points:0}]))}
export function schedule(clubs){const list=clubs.map(c=>c.id), rounds=[];for(let r=0;r<clubs.length-1;r++){const games=[];for(let i=0;i<clubs.length/2;i++){const a=list[i],b=list[list.length-1-i];games.push({round:r+1,home:r%2?b:a,away:r%2?a:b});}list.splice(1,0,list.pop());rounds.push(...games);}return rounds.concat(rounds.map(g=>({round:g.round+clubs.length-1,home:g.away,away:g.home})));}
export function formationBonus(formation){return {'4-3-3':2,'4-4-2':0,'3-5-2':1,'5-3-2':-1}[formation]??0;}
export function tacticStrength(base,tactic={}){const style={attacking:2,balanced:0,defensive:-1,possession:1}[tactic.style]??0;return base+formationBonus(tactic.formation)+style;}
export function result(home,away,rng=Math.random,homeTactic={},awayTactic={}){const h=Math.max(.2,tacticStrength(home.strength,homeTactic)/75),a=Math.max(.2,tacticStrength(away.strength,awayTactic)/75);return [Math.min(5,Math.floor(rng()*2.4*h+rng()*1.3)),Math.min(5,Math.floor(rng()*2.1*a))];}
export function playMatch(state,homeId,awayId,rng=Math.random){const home=state.clubs[homeId],away=state.clubs[awayId];const [hg,ag]=result(home,away,rng,state.tactics?.[homeId]||{},state.tactics?.[awayId]||{});const h=state.table[homeId],a=state.table[awayId];for(const t of [h,a])t.played++;h.goalsFor+=hg;h.goalsAgainst+=ag;a.goalsFor+=ag;a.goalsAgainst+=hg;if(hg>ag){h.wins++;h.points+=3;a.losses++;}else if(hg<ag){a.wins++;a.points+=3;h.losses++;}else{h.draws++;a.draws++;h.points++;a.points++;}return {home:home.name,away:away.name,hg,ag,homeId,awayId};}
export function playRound(state,round,rng=Math.random){const games=schedule(Object.values(state.clubs)).filter(g=>g.round===round);return games.map(g=>playMatch(state,g.home,g.away,rng));}
export function tableRows(state){return Object.values(state.table).sort((a,b)=>b.points-a.points||(b.goalsFor-b.goalsAgainst)-(a.goalsFor-a.goalsAgainst)||b.goalsFor-a.goalsFor).map((r,i)=>({...r,pos:i+1,club:state.clubs[r.clubId]}));}
